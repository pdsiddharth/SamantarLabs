# Copy to .env before the first `docker compose up -d`.

# The apex. Every hostname in Caddyfile and sites/*.caddy is built from it, so
# a staging droplet needs no other edits.
ROOT_DOMAIN=samantarlabs.com

# Where Let's Encrypt sends expiry warnings if renewal ever stops working.
ACME_EMAIL=siddharthjagtap1@gmail.com
